/// list on enums
///Enums are a special kind of class used to represent a fixed number of constant value
///
library;

enum TextSizes { small, medium, large }

enum OrderStatus { processing, shiped, delivered }

enum PaymentMethods { paypal, googlePay, applePay, visa, masterCard, redotPay }
