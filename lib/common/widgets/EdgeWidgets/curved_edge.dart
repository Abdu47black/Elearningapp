// import 'package:fidel/common/widgets/CustomShapes/reusableShapes/clip_path.dart';
import 'package:flutter/material.dart';

class MCurvedEdgeWidgets extends StatelessWidget {
  const MCurvedEdgeWidgets({
    super.key,
    this.child,
  });
  final Widget? child;
  @override
  Widget build(BuildContext context) {
    return Container(child: child);
  }
}
